apiVersion: apps/v1
kind: Deployment
metadata:
  name: border-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: border-postgres
  template:
    metadata:
      labels:
        app: border-postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15
        env:
        - name: POSTGRES_DB
          value: borderflow
        - name: POSTGRES_USER
          value: postgres
        - name: POSTGRES_PASSWORD
          value: borderflow123
        ports:
        - containerPort: 5432
        volumeMounts:
        - name: init-sql
          mountPath: /docker-entrypoint-initdb.d
      volumes:
      - name: init-sql
        configMap:
          name: borderflow-sql
          items:
          - key: schema.sql
            path: 01-schema.sql
          - key: seed.sql
            path: 02-seed.sql
---
apiVersion: v1
kind: Service
metadata:
  name: border-postgres
spec:
  selector:
    app: border-postgres
  ports:
  - port: 5432
    targetPort: 5432
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: border-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: border-app
  template:
    metadata:
      labels:
        app: border-app
    spec:
      containers:
      - name: app
        image: docker.io/library/borderflow-app:latest
        imagePullPolicy: Never
        env:
        - name: DB_USER
          value: postgres
        - name: DB_HOST
          value: border-postgres
        - name: DB_NAME
          value: borderflow
        - name: DB_PASSWORD
          value: borderflow123
        - name: DB_PORT
          value: "5432"
        - name: SITE_ID
          value: "2"
        - name: PORT
          value: "3000"
        - name: JWT_SECRET
          value: "c631128b62242842590f29b3db70cb466d2965b54b486c286efdb5bede374ccb"
        - name: SYNC_SECRET
          value: "borderflow-shared-sync-secret-2026"
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: border-app
spec:
  type: NodePort
  selector:
    app: border-app
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30002